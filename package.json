{
  "name": "mmk-motors-market-key",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"cd mmk && npm run dev\" \"cd mmk/server && npm run dev\"",
    "install-all": "npm install && cd mmk && npm install && cd ../mmk/server && npm install"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "json-server": "^1.0.0-beta.3",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
